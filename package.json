{
  "name": "apple-ii",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "author": "Terence Ang (motionfxdesign)",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace=@apple2/mcp-server && node packages/mcp-server/dist/index.js & npm run dev --workspace=@apple2/app",
    "build": "npm run build --workspace=@apple2/core && npm run build --workspace=@apple2/worker && npm run build --workspace=@apple2/app && npm run build --workspace=@apple2/mcp-server && npm run build --workspace=@apple2/server",
    "serve": "npm run build && node packages/server/dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:all": "npm run typecheck && npm run lint && vitest run",
    "typecheck": "tsc -b",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.0",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
